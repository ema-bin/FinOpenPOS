/**
 * Categorías: listado en DB (tabla categories).
 * Usar GET /api/categories?type=libre o type=damas para obtener las opciones.
 */
export type { Category, CategoryType } from "@/models/db/category";
